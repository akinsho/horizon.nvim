{
  "colors": {
    "foreground": "{{ syntax.gray }}"
  },
  "tokenColors": [
    {
      "scope": "punctuation.separator",
      "settings": {
        "foreground": "{{ syntax.gray }}{{ alpha.med }}"
      }
    }
  ]
}
